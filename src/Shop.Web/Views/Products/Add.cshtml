@model AddOrUpdateProductViewModel

<div class="row">
    <div class="col-md-4 col-lg-3">
        <form asp-action="Add" method="post">
            @Html.Partial("_AddOrUpdateProduct", Model)
            <button type="submit" class="btn btn-success">Add product</button>
        </form>
    </div>
</div>

